<Page x:Class="ServerMultiTool.Views.Pages.SettingsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:controls="clr-namespace:ServerMultiTool.Views.Controls"
      xmlns:pages="clr-namespace:ServerMultiTool.ViewModels.Pages"
      xmlns:settings="clr-namespace:ServerMultiTool.ViewModels.Pages.Settings"
      mc:Ignorable="d" 
      d:DesignHeight="750" d:DesignWidth="700"
      Title="Pipeline" 
      FontFamily="Poppins" 
      d:DataContext="{d:DesignInstance Type=settings:SettingsViewModel, IsDesignTimeCreatable=True}"
      >
    
    <Border CornerRadius="8" Background="{DynamicResource SecondaryBackgroundColor}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            
            <controls:GeneralInfoControl Grid.Row="0" 
                                         Margin="10" 
                                         DataContext="{Binding .GeneralInfo}"/>
            
            <TabControl Grid.Row="1"  
                        Margin="10,10,10,0"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        BorderThickness="0">
                <TabControl.Resources>
                    <Style TargetType="TabItem" BasedOn="{StaticResource {x:Type TabItem}}">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border Name="Border" 
                                            BorderThickness="1,1,1,0" 
                                            BorderBrush="Gainsboro" 
                                            CornerRadius="4,4,0,0" 
                                            Margin="2,0,2,-1"
                                            Padding="0,0,0,2">
                                        <ContentPresenter x:Name="ContentSite"
                                                          VerticalAlignment="Center"
                                                          HorizontalAlignment="Center"
                                                          ContentSource="Header"
                                                          Margin="10,2"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Panel.ZIndex" Value="1"/>
                                            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextColor}"/>
                                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource TertiaryBackgroundColor}"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="False">
                                            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextColor}"/>
                                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource SecondaryBackgroundColor}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>
                <TabItem Header="Main Settings">
                    <Border BorderThickness="0.8" BorderBrush="Gainsboro" Padding="10">
                        <Grid >
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Label Grid.Row="0"
                                    FontSize="14"
                                    Foreground="{DynamicResource PrimaryTextColor}"
                                    Content="Solution Directories:" 
                                    Margin="0,0,0,0"/>
                                <ListBox Grid.Row="1"
                                         Height="190"
                                         ItemsSource="{Binding SolutionDirectories}" 
                                         SelectedItem="{Binding SelectedSolutionDirectory}" 
                                         Background="{DynamicResource TertiaryBackgroundColor}"
                                         Foreground="{DynamicResource PrimaryTextColor}"
                                         Padding="0,0,0,0"
                                         Margin="0,0,0,0">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Vertical" Margin="0,5,0,5">
                                                <TextBox Text="{Binding Name}" 
                                                         FontWeight="Bold" 
                                                         VerticalAlignment="Center"
                                                         Margin="0,0,0,5"
                                                         Background="Transparent"
                                                         Foreground="{DynamicResource PrimaryTextColor}"
                                                         BorderThickness="0"
                                                         />
                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                                    <TextBlock Text="{Binding Path}" 
                                                               VerticalAlignment="Bottom"
                                                               Foreground="{DynamicResource SecondaryTextColor}"
                                                               />
                                                    <Button Content="..." 
                                                            Command="{Binding DataContext.SelectDirectoryCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                            CommandParameter="{Binding}" 
                                                            Width="15" 
                                                            Height="15" 
                                                            Margin="5,0,0,0"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            <StackPanel Grid.Row="2"
                                         Orientation="Horizontal" 
                                         HorizontalAlignment="Left" 
                                         VerticalAlignment="Top" 
                                         Margin="0,5,0,0">
                            
                                <Button Content="Add" 
                                        Command="{Binding AddSolutionDirectoryCommand}" 
                                        Style="{StaticResource CustomButtonStyle}"
                                        Width="75"
                                        Height="20"
                                        Margin="0,5,5,0"/>
                                <Button Content="Delete" 
                                        Style="{StaticResource CustomButtonStyle}"
                                        Command="{Binding RemoveSolutionDirectoryCommand}"
                                        CommandParameter="{Binding SelectedSolutionDirectory}"
                                        Width="75"
                                        Height="20"
                                        Margin="0,5,5,0"/>
                            </StackPanel>
                        </Grid>
                        
                        <Grid Grid.Row="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Label Grid.Row="0"
                                    Content="Http Directories:" 
                                    FontSize="14"
                                    Foreground="{DynamicResource PrimaryTextColor}"
                                    Margin="0,15,0,0"/>
                                <ListBox Grid.Row="1"
                                         Height="190"
                                         ItemsSource="{Binding HttpDirectories}" 
                                         SelectedItem="{Binding SelectedHttpDirectory}" 
                                         Background="{DynamicResource TertiaryBackgroundColor}"
                                         Foreground="{DynamicResource PrimaryTextColor}"
                                         Padding="0,0,0,0"
                                         Margin="0,0,0,0">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Vertical" Margin="0,5,0,5">
                                                <TextBox Text="{Binding Name}" 
                                                         FontWeight="Bold" 
                                                         VerticalAlignment="Center"
                                                         Margin="0,0,0,5"
                                                         Background="Transparent"
                                                         Foreground="{DynamicResource PrimaryTextColor}"
                                                         BorderThickness="0">
                                                </TextBox>
                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                                    <TextBlock Text="{Binding Path}" 
                                                               VerticalAlignment="Bottom"
                                                               Foreground="{DynamicResource SecondaryTextColor}"
                                                               />
                                                <Button Content="..." 
                                                        Command="{Binding DataContext.SelectDirectoryCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                        CommandParameter="{Binding}" 
                                                        Width="15" 
                                                        Height="15" 
                                                        Margin="5,0,0,0"/>
                                            </StackPanel>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            <StackPanel Grid.Row="2"
                                         Orientation="Horizontal" 
                                         HorizontalAlignment="Left" 
                                         VerticalAlignment="Top" 
                                         Margin="0,5,0,0">
                            
                                <Button Content="Add" 
                                        Command="{Binding AddHttpDirectoryCommand}" 
                                        Style="{StaticResource CustomButtonStyle}"
                                        Width="75"
                                        Height="20"
                                        Margin="0,5,5,0"/>
                                <Button Content="Delete" 
                                        Command="{Binding RemoveHttpDirectoryCommand}"
                                        CommandParameter="{Binding SelectedHttpDirectory}"
                                        Style="{StaticResource CustomButtonStyle}"
                                        Width="75"
                                        Height="20"
                                        Margin="0,5,5,0"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                    </Border>
                </TabItem>
                <TabItem Header="Pipeline Profiles">
                    <Border BorderThickness="0.8" BorderBrush="Gainsboro" Padding="10">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            
                            <Grid Grid.Row="0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                
                                <Label Grid.Row="0"
                                       FontSize="14"
                                       Foreground="{DynamicResource PrimaryTextColor}"
                                       Content="Pipeline Profiles:"
                                       Margin="0,0,0,0"/>
                                
                                <ListBox Grid.Row="1"
                                         Height="140"
                                         ItemsSource="{Binding PipelineProfiles}"
                                         SelectedItem="{Binding SelectedPipelineProfile, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         IsSynchronizedWithCurrentItem="True"
                                         Background="{DynamicResource TertiaryBackgroundColor}"
                                         Foreground="{DynamicResource PrimaryTextColor}"
                                         Padding="0,0,0,0"
                                         Margin="0,0,0,0">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Vertical" Margin="0,5,0,5">
                                                <TextBox Text="{Binding Name, UpdateSourceTrigger=LostFocus}"
                                                         FontWeight="Bold"
                                                         VerticalAlignment="Center"
                                                         Margin="0,0,0,5"
                                                         Background="Transparent"
                                                         Foreground="{DynamicResource PrimaryTextColor}"
                                                         BorderThickness="0"/>
                                                <ItemsControl ItemsSource="{Binding Operations}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <TextBlock Text="{Binding DisplayName}"
                                                                      Foreground="{DynamicResource SecondaryTextColor}"
                                                                      Margin="10,2,0,0"/>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                                
                                <StackPanel Grid.Row="2"
                                           Orientation="Horizontal"
                                           HorizontalAlignment="Left"
                                           VerticalAlignment="Top"
                                           Margin="0,5,0,0">
                                    <Button Content="Add"
                                            Command="{Binding AddPipelineProfileCommand}"
                                            Style="{StaticResource CustomButtonStyle}"
                                            Width="75"
                                            Height="20"
                                            Margin="0,5,5,0"/>
                                    <Button Content="Delete"
                                            Command="{Binding RemovePipelineProfileCommand}"
                                            CommandParameter="{Binding SelectedPipelineProfile}"
                                            Style="{StaticResource CustomButtonStyle}"
                                            Width="75"
                                            Height="20"
                                            Margin="0,5,5,0"/>
                                </StackPanel>
                            </Grid>

                            <Grid Grid.Row="1" Margin="0,15,0,0" >
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                            
                                <Label Grid.Row="0"
                                       Content="Edit Pipeline Profile"
                                       FontSize="14"
                                       Foreground="{DynamicResource PrimaryTextColor}"/>
                                
                                <controls:EditPipelineProfileControl 
                                    Grid.Row="1"
                                    Background="{DynamicResource TertiaryBackgroundColor}"
                                    DataContext="{Binding .EditPipelineProfile}"
                                    Height="270"/>
                            </Grid>
                        </Grid>
                    </Border>
                </TabItem>
            </TabControl>
            
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" 
                        Command="{Binding CancelSettingsCommand}" 
                        IsEnabled="{Binding HasUnsavedChanges}"
                        Style="{StaticResource CustomButtonStyle}"
                        Width="75"
                        Height="40"
                        Margin="0,10,10,10"/>
                <Button Content="Save" 
                        Command="{Binding SaveSettingsCommand}"
                        IsEnabled="{Binding HasUnsavedChanges}"
                        Style="{StaticResource CustomButtonStyle}"
                        Width="75"
                        Height="40"
                        Margin="0,10,10,10"/>
            </StackPanel>
        </Grid>
    </Border>
</Page>